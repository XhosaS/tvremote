<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/on_device_loading_card_height">
    <LinearLayout
        android:layout_gravity="center"
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <ProgressBar
            android:layout_gravity="center_horizontal"
            android:id="@+id/on_device_loading"
            android:layout_width="@dimen/on_device_loading_progress_size"
            android:layout_height="@dimen/on_device_loading_progress_size"
            android:indeterminate="true"
            android:indeterminateTint="@color/G800"
            android:indeterminateTintMode="src_in"
            style="@android:style/Widget.Material.Light.ProgressBar.Large"/>
        <TextView
            android:textAppearance="@style/OnDeviceLoadingMessage"
            android:layout_gravity="center_horizontal"
            android:id="@+id/on_device_loading_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_salmon"/>
    </LinearLayout>
</FrameLayout>
